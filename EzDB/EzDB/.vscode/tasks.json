{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-o",
                "main", // Name of the output file you desire
                "*.c",         // Compiles all C files in the directory
                "-lm"          // Link math library, remove if not needed
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
